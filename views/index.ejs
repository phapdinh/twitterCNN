<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="utf-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
	<meta http-equiv="refresh" content="30">
    <meta name="viewport" content="width=device-width, initial-scale=1">
    <!-- The above 3 meta tags *must* come first in the head; any other head content must come *after* these tags -->
    <title>Twitter CNN</title>
    <!-- Bootstrap -->
    <link href="bower_components/bootstrap/dist/css/bootstrap.min.css" rel="stylesheet">
</head>
<body>
	<div class="container">
        <div class="row">
            <div class="col-lg-12">
                <h1 align="center"> Twitter Feed </h1>
            </div>
        </div>
		<div class="row">
			<div class="col-lg-12">
				<div class="input-group">
					<input type="text" class="form-control" placeholder="Search for..." id="input-field">
					<span class="input-group-btn">
						<button class="btn btn-primary" type="button" id="submit">Search</button>
						<button class="btn btn-secondary" type="button" id="reload">Reload</button>
					</span>
				</div>
			</div>
		</div>
		<div class="row">
			<div class="col-lg-12">
				<table class="table table-hover">
					<thead>
						<tr>
							<th>
							Tweets
							</th>
						</tr>
					</thead>
					<tbody>
						<% for(var i=0; i < data.length; i++) { %>
							<tr>
								<td><%= data[i].text %></td>
								<td><%= data[i].coordinates %></td>
							</tr>
						<% } %>
					</tbody>
				</table>
			</div>
		</div>
	</div>
	<!-- jQuery (necessary for Bootstrap's JavaScript plugins) -->
	<script src="javascripts/bundle.js"></script>
</body>
</html>